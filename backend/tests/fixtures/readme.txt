This directory is designated for static test asset files. 
Since our tests generate binary fixtures (PDFs, DOCXs, Excel files) dynamically to prevent git repository bloat, this folder serves as a fallback location for custom offline test resources.
